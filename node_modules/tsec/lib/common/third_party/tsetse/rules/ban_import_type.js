"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Rule = void 0;
const ts = require("typescript");
const error_code_1 = require("../error_code");
const failure_1 = require("../failure");
const rule_1 = require("../rule");
/**
 * Bans 'import type'. google3 uses compiler options that elide imports that are
 * only used in a type position, so there is no need to ever use import type.
 */
class Rule extends rule_1.AbstractRule {
    constructor() {
        super(...arguments);
        this.ruleName = Rule.RULE_NAME;
        this.code = error_code_1.ErrorCode.BAN_IMPORT_TYPE;
    }
    register(checker) {
        checker.on(ts.SyntaxKind.ImportDeclaration, checkImport, this.code);
        checker.on(ts.SyntaxKind.ExportDeclaration, checkExport, this.code);
        checker.on(ts.SyntaxKind.ImportEqualsDeclaration, checkImportEquals, this.code);
    }
}
exports.Rule = Rule;
Rule.RULE_NAME = 'ban-import-type';
function checkImport(checker, importDeclaration) {
    if (!importDeclaration.importClause)
        return; // side effect import
    if (!importDeclaration.importClause.isTypeOnly)
        return;
    return checkPort(checker, importDeclaration.importClause);
}
function checkImportEquals(checker, importEqualsDeclaration) {
    if (!importEqualsDeclaration.isTypeOnly)
        return;
    return checkPort(checker, importEqualsDeclaration);
}
function checkExport(checker, exportDeclaration) {
    if (!exportDeclaration.isTypeOnly)
        return;
    return checkPort(checker, exportDeclaration);
}
function checkPort(checker, parentNode) {
    const sourceFile = parentNode.getSourceFile();
    const typeKeyword = parentNode.getChildren(sourceFile)
        .find(t => t.kind === ts.SyntaxKind.TypeKeyword);
    if (!typeKeyword) {
        throw new Error('import type must have a type keyword: ' + parentNode.getFullText());
    }
    // Remove the type keyword.
    const fix = (0, failure_1.replaceNode)(typeKeyword, '');
    const importExport = parentNode.kind === ts.SyntaxKind.ImportClause ? 'import' : 'export';
    checker.addFailureAtNode(typeKeyword, `do not use ${importExport} type, use regular ${importExport}s.`, Rule.RULE_NAME, undefined, [fix]);
}
//# sourceMappingURL=ban_import_type.js.map